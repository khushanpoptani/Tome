{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "server-dashboard",
  "description": "Core permissions for the Tome Server dashboard window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
